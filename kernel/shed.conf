# shed's own additions on top of Kata's fragments. Kata is the known-good
# base we build from, not a target to match; anything shed needs that Kata
# does not ship goes here.
CONFIG_LOCALVERSION="-shed"
